version: '3.8'

services:
  imutflix-backend:
    build: .
    container_name: imutflix-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./uploads:/app/uploads
